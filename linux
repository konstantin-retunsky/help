cd          открыть папку
cd ..       назад в папку 
mkdir   создать папку
ls           каталог
ls  -l       показывает все файлы, без папок. Вид таблицей.
ls -a       показывает все файлы, включая папки точками пишет имена.
ls -a -l   лучший вариант )
pwd      где я
mkdir (имя папки) => создание папки
> (путь к папке затем имя файла и тип, либо сразу имя, если в находишься в папке) => создание файла
cp имя файла новое имя копии => копирование файла 
mw (старое имя новое имя) => переименовать 
rm (имя файлы) => удаление файла 
rm *.pdf => удаление определеного типа файла
rmdir (имя папки) => удаление папки
rm -rf (имя папки) =>  удаление папки с файлами внутри нее
cat => выводит содержимое файла 
echo 'текст' > имя файла
file (имя) => что это за файл
find (имя файла) => поиск
ps => процессы в терменале. 
kill -9 номер закрывает процесс в терменале
!! если забыл sudo то поможет)
look ввести слово половину и будут показаны варианты слов 
cd && ls -l
sh имя файла => выполняет команды в нем
reboot - перезагрузка
имя приложение файл который нужно открыть и открывается файл)
convert имя файла имя файла имя файла  имя файла в который нужно конвертировать
ls - информация попки

unoconv -f pdf Documents/test/\!Контр№2_Вывод\ в\ DIV/задание.doc - конвертр в pdf

mkdir -p /mnt/d/test/{1,2,3}
touch /mnt/d/test/{1/test.1,2/test.2,3/test.4}
unzip filename.zip (-l вывести таблицу разархивированного) (-d /path разархивировать в указанную дирикторию)

pidof firefox - нахождение pid приложения
ps -fHu bogeyman - показ процессов, где
-f - вывести максимум доступных данных, например, количество потоков;
-H - отображать дерево процессов linux;
-u, (U) - выбрать процессы пользователя.


https://github.com/nodesource/distributions/blob/master/README.md - установка последней версии nodejs
